[
  {
    "id": 1,
    "title": "Two Sum",
    "category": "Array & Hashing",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 13,
    "description": "The naive approach uses a nested loop (brute force) to check every possible pair of numbers to see which two add up to the target.[cite: 2] This results in a time complexity of O(n^2), making it inefficient and too slow for large datasets.[cite: 2] We need a way to avoid repeating checks and find the required number instantly.[cite: 2]",
    "solution_logic": "Use a Hash Map (dictionary) to keep track of the numbers we have already visited, allowing us to parse the list in a single pass.[cite: 2] For each element, we calculate its complement (target - current_number).[cite: 2] If the complement already exists in our dictionary, we have found our pair and return their indices.[cite: 2] If not, we store the current number and its index in the dictionary for future lookups.[cite: 2] This optimizes the time complexity down to O(n).[cite: 2]",
    "python_code": "class Solution:\n    def twoSum(self, nums: List[int], target: int) -> List[int]:\n        visited = {}\n        \n        list_length = len(nums)\n        for i in range(list_length):\n            curr = nums[i]\n            nr = target - curr\n            if nr in visited:\n                return [visited[nr], i]\n\n            visited[curr] = i\n\n        return []"
  },
  {
    "id": 217,
    "title": "Contains Duplicate",
    "category": "Array & Hashing",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 9,
    "description": "Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.[cite: 2] The naive approach involves comparing every element against all other elements using nested loops (O(n^2) time) or sorting the array first (O(n log n) time), which becomes inefficient for large datasets.[cite: 2] We need a way to detect duplicate elements in a single pass.[cite: 2]",
    "solution_logic": "Use a Hash Set to keep track of the numbers we have already seen.[cite: 2] A Set is a data structure that does not allow duplicates and provides instant lookups.[cite: 2] As we iterate through the array, we check if the current number is already in our set.[cite: 2] If it is, a duplicate exists, so we return true.[cite: 2] If it is not, we add it to the set and continue.[cite: 2] This optimizes the time complexity down to O(n) and the space complexity to O(n).[cite: 2]",
    "python_code": "class Solution:\n    def containsDuplicate(self, nums: List[int]) -> bool:\n        visited = set()\n\n        for i in nums:\n            if i in visited:\n                return True\n            visited.add(i)\n\n        return False"
  },
  {
    "id": 242,
    "title": "Valid Anagram",
    "category": "Array & Hashing",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 22,
    "description": "Given two strings s and t, return true if t is an anagram of s, and false otherwise.[cite: 2] An anagram is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.[cite: 2] The naive approach is to sort both strings and compare them, which takes O(n log n) time.[cite: 2] We need a more optimal, O(n) time complexity solution.[cite: 2]",
    "solution_logic": "We can use Hash Maps (dictionaries) to count the frequency of each character in both strings.[cite: 2] First, we check if the lengths of the strings are equal; if they are not, it is impossible for them to be anagrams.[cite: 2] Then, we iterate through each string, keeping a tally of how many times each letter appears.[cite: 2] Finally, we compare the two dictionaries.[cite: 2] If the frequency of every letter matches perfectly, the strings are anagrams.[cite: 2] This approach optimizes the time complexity down to O(n) and the space complexity to O(n).[cite: 2]",
    "python_code": "class Solution:\n    def isAnagram(self, s: str, t: str) -> bool:\n        s_letters = {}\n        t_letters = {}\n\n        if len(s) != len(t):\n            return False\n\n        for l in s:\n            if l in s_letters:\n                s_letters[l] += 1\n            else:\n                s_letters[l] = 1\n        \n        for l in t:\n            if l in t_letters:\n                t_letters[l] += 1\n            else:\n                t_letters[l] = 1\n\n        if s_letters == t_letters:\n            return True\n        return False"
  },
  {
    "id": 49,
    "title": "Group Anagrams",
    "category": "Array & Hashing",
    "difficulty": "Medium",
    "time_complexity": "O(n * k log k)",
    "space_complexity": "O(n * k)",
    "loc": 16,
    "description": "Given an array of strings strs, group the anagrams together.[cite: 2] You can return the answer in any order.[cite: 2] The naive approach would be to compare every word with every other word to check if they are anagrams, but this is highly inefficient.[cite: 2] We need a way to group them in a single pass.[cite: 2]",
    "solution_logic": "The key insight is that if two words are anagrams, they will look exactly the same if we sort their letters alphabetically (e.g., \"eat\" and \"tea\" both become \"aet\" when sorted).[cite: 2] We can use a Hash Map (dictionary) where the \"key\" is the sorted word, and the \"value\" is a list of the original words that match that key.[cite: 2]\nAs we iterate through the list, we sort each word, check if its sorted version exists in our dictionary, and group it accordingly.[cite: 2] This approach has a time complexity of O(n * k log k), where n is the number of words and k is the maximum length of a word (due to the sorting step).[cite: 2] The space complexity is O(n * k) to store the dictionary.[cite: 2]",
    "python_code": "class Solution:\n    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:\n        groups = {}\n\n        for s in strs:\n            freq = [0] * 26\n            for l in s:\n                pos = ord(l) - ord('a')\n                freq[pos] += 1\n\n            key = tuple(freq)\n            if key in groups:\n                groups[key].append(s)\n            else:\n                groups[key] = [s]\n\n        groups_list = list(groups.values())\n        return groups_list"
  },
  {
    "id": 347,
    "title": "Top k Frequent Elements",
    "category": "Array & Hashing",
    "difficulty": "Medium",
    "time_complexity": "O(n log n)",
    "space_complexity": "O(n)",
    "loc": 19,
    "description": "Given an integer array nums and an integer k, return the k most frequent elements.[cite: 2] You may return the answer in any order.[cite: 2] The challenge is to count how many times each number appears and then reliably extract the ones with the highest counts.[cite: 2]",
    "solution_logic": "We can solve this using a clear, step-by-step approach.[cite: 2] First, we use a Hash Map (dictionary) to count the frequency of each number.[cite: 2] Next, we convert this dictionary into a list of pairs in the format (frequency, number).[cite: 2] By placing the frequency first, we can use the built-in sorting function, which automatically sorts tuples based on their first element.[cite: 2] We sort this list in descending order (from most frequent to least frequent) and finally extract the numbers from the top k pairs.[cite: 2]\nThis approach has a time complexity of O(n log n) due to the sorting step, and a space complexity of O(n) to store the dictionary and the list of pairs.[cite: 2] It is highly readable and avoids overly complex language-specific syntax.[cite: 2]",
    "python_code": "class Solution:\n    def topKFrequent(self, nums: List[int], k: int) -> List[int]:\n        freq = {}\n        for i in nums:\n            if i in freq:\n                freq[i] += 1\n            else:\n                freq[i] = 1\n\n        pairs = []\n        for nr, nr_app in freq.items():\n            pairs.append((nr_app, nr))\n\n        pairs.sort(reverse=True)\n\n        ret = []\n        for i in range(k):\n            ret.append(pairs[i][1])\n\n        return ret"
  },
  {
    "id": 238,
    "title": "Product of Array Except Self",
    "category": "Array & Hashing",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 18,
    "description": "Given an integer array nums, return an array answer such that answer[i] is equal to the product of all the elements of nums except nums[i].[cite: 2] The algorithm must run in O(n) time and without using the division operation.[cite: 2] The naive approach would be to use nested loops to multiply all other numbers for each element (O(n^2) time), which is too slow.[cite: 2]",
    "solution_logic": "Since we cannot use division, we can solve this by calculating the products of the numbers to the \"left\" of our current index, and the products of the numbers to the \"right\".[cite: 2]\nWe create two separate arrays: one to store the left products and one for the right products.[cite: 2] For any given number at index i, the product of the array except itself is simply left_products[i] * right_products[i].[cite: 2] \nWe first loop through the array left-to-right to build the left products, then right-to-left to build the right products, and finally multiply them together.[cite: 2] This ensures we only pass through the arrays a few times sequentially, giving us the required O(n) time complexity.[cite: 2]",
    "python_code": "class Solution:\n    def productExceptSelf(self, nums: List[int]) -> List[int]:\n        nr = len(nums)\n\n        nums1 = [1] * nr\n        nums2 = [1] * nr\n        ret = [1] * nr\n\n        for i in range(1, nr):\n            nums1[i] = nums1[i-1] * nums[i - 1]\n        \n        for i in range (nr - 2, -1, -1):\n            nums2[i] = nums2[i + 1] * nums[i + 1]\n\n        for i in range(nr):\n            ret[i] = nums1[i] * nums2[i]\n\n        return ret"
  },
  {
    "id": 128,
    "title": "Longest Consecutive Sequence",
    "category": "Array & Hashing",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 17,
    "description": "Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence.[cite: 2] The algorithm must run in O(n) time complexity.[cite: 2] The naive approach would be to sort the array first and then count the longest sequence, which would take O(n log n) time.[cite: 2] We need a faster solution that avoids sorting altogether.[cite: 2]",
    "solution_logic": "To achieve O(n) time complexity, we can convert the list into a Hash Set.[cite: 2] A Set allows us to look up numbers instantly.[cite: 2] The main trick is to identify the \"start\" of a sequence.[cite: 2] A number is the start of a sequence ONLY if the number strictly before it (number - 1) does not exist in our set.[cite: 2]\nOnce we find a starting number, we simply keep checking if the next consecutive number (number + 1) exists in the set, counting how long the sequence gets.[cite: 2] By only building sequences from their true starting points, we ensure that each number is visited at most twice, keeping our time complexity at O(n).[cite: 2]",
    "python_code": "class Solution:\n    def longestConsecutive(self, nums: List[int]) -> int:\n        nr = set(nums)\n        seq_max = 0\n\n        for i in nr:\n            if (i - 1) not in nr:\n                curr_length = 1\n                curr = i\n\n                while (curr + 1) in nr:\n                    curr += 1\n                    curr_length += 1\n\n                if curr_length > seq_max:\n                    seq_max = curr_length\n\n        return seq_max"
  },
  {
    "id": 125,
    "title": "Valid Palindrome",
    "category": "Two Pointers",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 22,
    "description": "Given a string s, return true if it is a palindrome, or false otherwise.[cite: 2] A palindrome reads the same forward and backward.[cite: 2] We must ignore all non-alphanumeric characters (spaces, punctuation) and convert all uppercase letters to lowercase.[cite: 2] The naive approach is to build a brand-new string by filtering out the bad characters and then comparing it to its reverse.[cite: 2] While this takes O(n) time, it also takes O(n) extra memory to store the new string.[cite: 2] We want an optimal solution with O(1) space complexity.[cite: 2]",
    "solution_logic": "We can solve this elegantly using the \"Two Pointers\" technique.[cite: 2] We place one pointer at the beginning of the string (left) and one at the end (right).[cite: 2] We move them towards the center simultaneously.[cite: 2]\nIf either pointer points to a space or a punctuation mark, we simply skip it by moving the pointer forward (or backward).[cite: 2] Once both pointers land on valid alphanumeric characters, we compare them in lowercase.[cite: 2] If they mismatch, it's not a palindrome.[cite: 2] We repeat this until the two pointers meet in the middle.[cite: 2] This guarantees O(n) time complexity while using absolutely no extra memory (O(1) space), because we are just evaluating the original string in place.[cite: 2]",
    "python_code": "class Solution:\n    def isPalindrome(self, s: str) -> bool:\n        def is_alphanumeric(c):\n            if ('a' <= c <= 'z') or ('A' <= c <= 'Z') or ('0' <= c <= '9'):\n                return True\n            return False\n        s_length = len(s)\n        left = 0\n        right = s_length - 1\n\n        while left < right:\n            while left < right and not is_alphanumeric(s[left]):\n                left += 1\n\n            while left < right and not is_alphanumeric(s[right]):\n                right -= 1\n\n            if s[left].lower() != s[right].lower():\n                return False\n\n            left += 1\n            right -= 1\n\n        return True"
  },
  {
    "id": 15,
    "title": "3Sum",
    "category": "Two Pointers",
    "difficulty": "Medium",
    "time_complexity": "O(n^2)",
    "space_complexity": "O(1)",
    "loc": 22,
    "description": "Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that they add up to exactly 0.[cite: 2] The solution set must not contain duplicate triplets.[cite: 2] A brute-force approach (checking every possible combination of 3 numbers) would require three nested loops, leading to a catastrophic O(n^3) time complexity.[cite: 2] We need a more efficient way to search through the array while easily avoiding duplicate sets.[cite: 2]",
    "solution_logic": "We can solve this efficiently in O(n^2) time by sorting the array first.[cite: 2] Sorting takes O(n log n) time, but it brings two massive benefits: it allows us to use the \"Two Pointers\" technique and makes skipping duplicate numbers trivial.[cite: 2]\nWe iterate through the sorted array.[cite: 2] For each number a, we want to find two other numbers (b and c) that sum to -a (so that a + b + c = 0).[cite: 2] We set a left pointer just after a, and a right pointer at the end of the array.[cite: 2]\nIf the total sum is greater than zero, we move the right pointer backward to get a smaller number.[cite: 2]\nIf the total sum is less than zero, we move the left pointer forward to get a larger number.[cite: 2]\nIf the sum is exactly zero, we record the triplet and move our pointers, ensuring we skip any consecutive duplicate numbers to prevent duplicate answers in our final list.[cite: 2]",
    "python_code": "class Solution:\n    def threeSum(self, nums: list[int]) -> list[list[int]]:\n        ret = []\n\n        nums.sort()\n        nums_length = len(nums)\n        for i in range(nums_length):\n            if i > 0 and nums[i] == nums[i-1]:\n                continue\n            left = i + 1\n            right = nums_length - 1\n\n            while left < right:\n                sum3 = nums[i] + nums[left] + nums[right]\n                if sum3 > 0:\n                    right -= 1\n                elif sum3 < 0:\n                    left += 1\n                else:\n                    ret.append([nums[i], nums[left], nums[right]])\n\n                    # skip the duplicates\n                    left += 1\n                    while left < right and nums[left] == nums[left - 1]:\n                        left += 1\n\n        return ret"
  },
  {
    "id": 11,
    "title": "Container With Most Water",
    "category": "Two Pointers",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 19,
    "description": "You are given an integer array height where height[i] represents the height of a vertical line at index i.[cite: 2] Find two lines that together with the x-axis form a container, such that the container holds the most water.[cite: 2] Return the maximum amount of water a container can store.[cite: 2] The brute-force solution would be to check every possible pair of lines (O(n^2) time), but we need a faster O(n) approach.[cite: 2]",
    "solution_logic": "We can efficiently solve this using the \"Two Pointers\" technique.[cite: 2] The amount of water between two lines is determined by two factors: the distance between them (the width) and the height of the shorter line (since water would spill over if we filled it past the shorter wall).[cite: 2]\nWe start by placing a left pointer at the beginning and a right pointer at the end of the array.[cite: 2] This gives us the maximum possible width.[cite: 2] We calculate the area and keep track of the maximum area seen so far.[cite: 2] To try and find a larger area, we must move our pointers inward.[cite: 2] Since the width is decreasing, the only way to get a larger area is to find taller lines.[cite: 2] Therefore, we always move the pointer that points to the shorter line, hoping to encounter a taller one.[cite: 2] We repeat this until the two pointers meet, resulting in a highly efficient O(n) time complexity and O(1) space.[cite: 2]",
    "python_code": "class Solution:\n    def maxArea(self, height: List[int]) -> int:\n        left = 0\n        height_length = len(height)\n        right = height_length - 1\n\n        maximum = 0\n        while left < right:\n            width = right - left\n            minim_height = min(height[left], height[right])\n\n            curr = width * minim_height\n            if curr > maximum:\n                maximum = curr\n            \n            if height[left] < height[right]:\n                left += 1\n            else:\n                right -= 1\n\n            \n        return maximum"
  },
  {
    "id": 121,
    "title": "Best Time To Buy and Sell Stock",
    "category": "Sliding Window",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 11,
    "description": "You are given an array prices where prices[i] is the price of a given stock on the i-th day.[cite: 2] You want to maximize your profit by choosing a single day to buy one stock and choosing a different day in the future to sell that stock.[cite: 2] Return the maximum profit you can achieve from this transaction.[cite: 2] If you cannot achieve any profit, return 0.[cite: 2] The naive approach (O(n^2)) checks every single buy-and-sell combination, which will result in a Time Limit Exceeded error for large inputs.[cite: 2] We need an O(n) solution.[cite: 2]",
    "solution_logic": "We can solve this efficiently using a simplified \"Sliding Window\" (or two pointers) concept in a single pass.[cite: 2] We only need to keep track of two things as we move through time (the array): the lowest buying price we have seen so far, and the maximum profit we can make.[cite: 2]\nAs we iterate through the prices day by day:If the current price is lower than our lowest recorded buying price, we update our buying price.[cite: 2] (We want to buy as cheap as possible).[cite: 2] Otherwise, we check if selling at the current price yields a better profit than our current maximum profit.[cite: 2] If it does, we update the maximum profit.[cite: 2] This guarantees we find the highest possible profit while only iterating through the list exactly once (O(n) time) and using O(1) extra space.[cite: 2]",
    "python_code": "class Solution:\n    def maxProfit(self, prices: List[int]) -> int:\n        min_price = float('inf')\n        max_profit = 0\n\n        for p in prices:\n            if p < min_price:\n                min_price = p\n            elif p - min_price > max_profit:\n                max_profit = p - min_price\n\n        return max_profit"
  },
  {
    "id": 3,
    "title": "Longest Substring Without Repeating Characters",
    "category": "Sliding Window",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 16,
    "description": "Given a string s, find the length of the longest substring without repeating characters.[cite: 2] A brute-force solution would be to check every possible substring and verify if it has duplicate characters, which leads to a terrible O(n^3) or O(n^2) time complexity.[cite: 2] We need an O(n) approach to pass the tests.[cite: 2]",
    "solution_logic": "This is the textbook scenario for the \"Sliding Window\" pattern combined with a Hash Set.[cite: 2] We define a window using two pointers: left and right.[cite: 2] We also use a Set to keep track of the characters currently inside our window.[cite: 2] We expand the window by moving the right pointer one character at a time.[cite: 2] If the new character is NOT in our Set, we add it, calculate the current window size (right - left + 1), and update our maximum length.[cite: 2] If the new character IS already in our Set, we have a duplicate.[cite: 2] We must shrink our window from the left by removing characters from the Set and moving the left pointer forward until the duplicate is completely removed.[cite: 2] This approach ensures each character is visited at most twice (once by the right pointer, once by the left), achieving a highly optimal O(n) time complexity.[cite: 2]",
    "python_code": "class Solution:\n    def lengthOfLongestSubstring(self, s: str) -> int:\n        visited = set()\n        left = 0\n        max_length = 0\n\n        s_length = len(s)\n        for i in range(s_length):\n            while s[i] in visited:\n                visited.remove(s[left])\n                left += 1\n\n            visited.add(s[i])\n            curr = i - left + 1\n            if curr > max_length:\n                max_length = curr\n\n        return max_length"
  },
  {
    "id": 424,
    "title": "Longest Repeating Character Replacement",
    "category": "Sliding Window",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 23,
    "description": "You are given a string s and an integer k.[cite: 2] You can choose any character of the string and change it to any other uppercase English character.[cite: 2] You can perform this operation at most k times.[cite: 2] Return the length of the longest substring containing the same letter you can get after performing these operations.[cite: 2] A brute-force approach would check every possible substring and count replacements, leading to an inefficient O(n^2) time complexity.[cite: 2] We need an optimal O(n) solution.[cite: 2]",
    "solution_logic": "We use the \"Sliding Window\" technique combined with a Hash Map to count the frequencies of characters inside our current window.[cite: 2] The core logic relies on a simple mathematical formula:Window_Length - Most_Frequent_Character_Count = Number_Of_Replacements_Needed.[cite: 2] If the number of replacements needed is less than or equal to k, our window is valid, and we can record its size.[cite: 2] If the replacements needed exceed k, our window is invalid.[cite: 2] When this happens, we shrink the window from the left by decrementing the count of the left character and moving the left pointer forward.[cite: 2] By constantly tracking the maximum frequency of a single character we've seen in the window (max_freq), we can evaluate the validity of the window in O(1) time at each step.[cite: 2] This allows us to traverse the string exactly once, achieving O(n) time complexity.[cite: 2]",
    "python_code": "class Solution:\n    def characterReplacement(self, s: str, k: int) -> int:\n        left = 0\n        s_length = len(s)\n        right = s_length - 1\n\n        freq = {}\n        max_length = 0\n        max_freq = 0\n\n        for i in range(s_length):\n            curr = s[i]\n            freq[curr] = freq.get(curr, 0) + 1\n            if max_freq < freq[curr]:\n                max_freq = freq[curr]\n\n            window_length = i - left + 1\n            while window_length - max_freq > k:\n                c1 = s[left]\n                freq[c1] -= 1\n                left += 1\n                window_length = i - left + 1\n\n            if window_length > max_length:\n                max_length = window_length\n\n        return max_length"
  },
  {
    "id": 643,
    "title": "Maxim Average Subarray",
    "category": "Sliding Window",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 11,
    "description": "You are given an integer array nums consisting of n elements, and an integer k.[cite: 2] Find a contiguous subarray whose length is equal to k that has the maximum average value and return this value.[cite: 2] A naive approach would be to calculate the sum of every possible group of k elements from scratch.[cite: 2] However, for a very large array, this leads to an inefficient O(n * k) time complexity.[cite: 2] We need a faster O(n) solution.[cite: 2]",
    "solution_logic": "This is the textbook problem for the Fixed-Size Sliding Window pattern.[cite: 2] Instead of recalculating the sum of k elements from scratch every time we move forward, we can just update our current sum.[cite: 2] First, we calculate the sum of the first k elements.[cite: 2] This is our initial \"window\".[cite: 2] Then, we slide the window one step to the right.[cite: 2] To get the new sum, we simply take our current sum, add the new element coming into the window from the right, and subtract the old element that just fell out of the window on the left.[cite: 2] We keep track of the maximum sum seen so far.[cite: 2] At the very end, we divide the maximum sum by k to get the maximum average.[cite: 2] This allows us to process the entire array in a single pass (O(n) time) using O(1) extra space.[cite: 2]",
    "python_code": "class Solution:\n    def findMaxAverage(self, nums: List[int], k: int) -> float:\n        curr_sum = sum(nums[:k])\n        max_sum = curr_sum\n\n        nums_length = len(nums)\n        for i in range (k, nums_length):\n            curr_sum = curr_sum + nums[i] - nums[i- k]\n\n            if curr_sum > max_sum:\n                max_sum = curr_sum\n\n        ret = max_sum / k\n        return ret"
  },
  {
    "id": 20,
    "title": "Valid Parentheses",
    "category": "Stack",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(n)",
    "loc": 22,
    "description": "We get a string made entirely of brackets: ()[]{}.[cite: 2] We need to figure out if they close in the correct order.[cite: 2] For example, ([]) is valid, but ([)] is broken because the square bracket closes before the inner parenthesis does.[cite: 2] We want to check this in a single pass without overcomplicating things.[cite: 2]",
    "solution_logic": "This is the classic use case for a Stack.[cite: 2] A stack works on a Last-In, First-Out (LIFO) principle.[cite: 2] You can think of it like a stack of plates—you always take the top one off first.[cite: 2] Here's the game plan: we read the string character by character.[cite: 2] If it's an opening bracket, we just toss it onto our stack.[cite: 2] If it's a closing bracket, we look at the very top of our stack.[cite: 2] If it's the correct matching opening bracket, we pop it off and move on.[cite: 2] If it's a mismatch, or if the stack is completely empty when we try to check, we know the string is invalid immediately and we return False.[cite: 2] To avoid writing a bunch of ugly if/elif statements for every bracket type, we map them out in a simple dictionary.[cite: 2] This keeps the code clean and runs in O(n) time and O(n) space.[cite: 2]",
    "python_code": "class Solution:\n    def isValid(self, s: str) -> bool:\n        stack = []\n\n        s_map = {\n            ')': '(', \n            '}': '{', \n            ']': '['\n        }\n\n        for c in s:\n            if c in s_map:\n                curr = '#'\n                if stack:\n                    curr = stack.pop()\n                \n                if s_map[c] != curr:\n                    return False\n            \n            else:\n                stack.append(c)\n\n        return not stack"
  },
  {
    "id": 155,
    "title": "Min Stack",
    "category": "Stack",
    "difficulty": "Medium",
    "time_complexity": "O(1)",
    "space_complexity": "O(n)",
    "loc": 36,
    "description": "We need to design a custom stack.[cite: 2] It has to do all the standard stack operations (push, pop, top), but with a catch: we also need a getMin() function that retrieves the smallest number in the stack instantly, in O(1) time.[cite: 2] The tricky part? If we pop the current minimum off the stack, the stack needs to immediately know what the new minimum is without scanning through all the remaining elements (which would be O(n)).[cite: 2]",
    "solution_logic": "To pull off that O(1) time requirement for finding the minimum, we can't just iterate through the stack every time getMin() is called.[cite: 2] Instead, we use a classic space-time tradeoff.[cite: 2] We cheat a little by maintaining two stacks under the hood.[cite: 2] The stack holds the actual values.[cite: 2] The min_stack runs parallel to it and keeps a historical record of what the minimum was at the exact moment each element was pushed.[cite: 2] When we push a new number, we add it to the main stack.[cite: 2] Then, we look at the top of our min_stack to see the current minimum.[cite: 2] We push whichever is smaller (the new number or the current minimum) onto the min_stack.[cite: 2] When we pop, we just pop from both stacks.[cite: 2] This guarantees that the top of our min_stack will always magically hold the correct minimum value for the current state of the main stack.[cite: 2]",
    "python_code": "class MinStack:\n\n    def __init__(self):\n        self.stack = []\n        self.min_stack = []\n\n    def push(self, value: int) -> None:\n        self.stack.append(value)\n        if not self.min_stack:\n            self.min_stack.append(value)\n        else:\n            curr = self.min_stack[-1]\n            if value < curr:\n                self.min_stack.append(value)\n            else:\n                self.min_stack.append(curr)\n\n    def pop(self) -> None:\n        if self.stack:\n            self.stack.pop()\n            self.min_stack.pop()\n\n    def top(self) -> int:\n        if self.stack:\n            return self.stack[-1]\n            return None\n\n    def getMin(self) -> int:\n        if self.min_stack:\n            return self.min_stack[-1]\n        return None"
  },
  {
    "id": 153,
    "title": "Find Minimum in Rotated Sorted",
    "category": "Binary Search",
    "difficulty": "Medium",
    "time_complexity": "O(log n)",
    "space_complexity": "O(1)",
    "loc": 14,
    "description": "We get an array of numbers that was originally sorted in ascending order, but someone took a chunk from the end and moved it to the front (rotated it).[cite: 2] We need to find the smallest number inside it.[cite: 2] The catch? We have to do it in O(log n) time.[cite: 2] This strict time limit means we can't just run a simple for loop to scan the array (which would be O(n)).[cite: 2]",
    "solution_logic": "Whenever you see \"sorted array\" and \"O(log n)\" in an interview, your brain should instantly scream: Binary Search![cite: 2] Normally, binary search is easy, but this array is twisted.[cite: 2] However, we can still use a left and right pointer and look at the mid element.[cite: 2] Because of the rotation, the array is basically split into two distinct sorted portions.[cite: 2] Here is the trick: we compare our mid number to our right number.[cite: 2] If the mid number is strictly greater than the right number, it means our mid is part of the larger chunk that was moved to the front.[cite: 2] Therefore, the true minimum must be somewhere to the right of it.[cite: 2] So, we move our left pointer to mid + 1.[cite: 2] On the flip side, if the mid number is smaller, the minimum is either at mid or somewhere to its left, so we pull our right pointer down to mid.[cite: 2] By continuously chopping the search space in half, we find the minimum lightning-fast.[cite: 2]",
    "python_code": "class Solution:\n    def findMin(self, nums: List[int]) -> int:\n        left = 0\n        nums_length = len(nums)\n        right = nums_length - 1\n\n        while left < right:\n            mid = (left + right) // 2\n\n            if nums[mid] > nums[right]:\n                left = mid + 1\n            else:\n                right = mid\n\n        return nums[left]"
  },
  {
    "id": 33,
    "title": "Search in Rotated Sorted Array",
    "category": "Binary Search",
    "difficulty": "Medium",
    "time_complexity": "O(log n)",
    "space_complexity": "O(1)",
    "loc": 24,
    "description": "We are given the same setup as before: an array of numbers that was sorted but then rotated at some pivot point.[cite: 2] However, this time we aren't just looking for the minimum.[cite: 2] We are given a target number, and we need to find its index.[cite: 2] If it doesn't exist in the array, we return -1.[cite: 2] Just like before, we are strictly bound to an O(log n) time complexity, which completely rules out a linear scan.[cite: 2]",
    "solution_logic": "Since we need O(log n) time, Binary Search is our only option.[cite: 2] But how do we binary search an array that isn't perfectly sorted? The core realization is this: if you take a rotated sorted array and split it exactly in half, at least one of those halves will always be perfectly sorted.[cite: 2] So, our strategy is to find the mid point and then check which half is the \"normal\" sorted one.[cite: 2] If the left half is perfectly sorted, we check if our target falls within its boundaries (between left and mid).[cite: 2] If it does, we ignore the right side and continue searching left.[cite: 2] If it doesn't, we know the target must be in the chaotic right half.[cite: 2] If the right half is perfectly sorted, we apply the exact same logic.[cite: 2] We check if the target sits nicely between mid and right.[cite: 2] If yes, we search right; if no, we search left.[cite: 2] By constantly identifying the sorted half and checking if our target belongs there, we can confidently chop our search space in half at every step.[cite: 2]",
    "python_code": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        left = 0\n        nums_length = len(nums)\n        right = nums_length - 1\n\n        while left <= right:\n            mid = (left + right) // 2\n\n            if nums[mid] == target:\n                return mid\n\n            if nums[left] <= nums[mid]:\n                if nums[left] <= target < nums[mid]:\n                    right = mid - 1\n                else:\n                    left = mid + 1\n            \n            else:\n                if nums[mid] < target <= nums[right]:\n                    left = mid + 1\n                else:\n                    right = mid - 1\n\n        return -1"
  },
  {
    "id": 206,
    "title": "Reverse Linked List",
    "category": "Linked List",
    "difficulty": "Easy",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 17,
    "description": "We get the head of a singly linked list, and we need to reverse it so the pointers point backward instead of forward, then return the new head.[cite: 2] We want to do this in a single pass with O(n) time complexity and O(1) extra space.[cite: 2]",
    "solution_logic": "Working with linked lists is all about pointer manipulation without losing your place.[cite: 2] To reverse a list, we need to change the next pointer of every node so it points to the previous node instead of the next one.[cite: 2] To pull this off cleanly, we keep track of three pointers: prev starts at None (because the old head will become the tail and point to nothing).[cite: 2] curr starts at the head of the list.[cite: 2] nxt is a temporary holder used inside the loop so we don't lose the rest of the list when we flip the pointer.[cite: 2] As we iterate through the list, we save the next node, flip the current node's pointer backward to prev, and then shift both prev and curr one step forward.[cite: 2] It’s a classic pointer-swapping loop that runs in O(n) time and uses zero extra memory.[cite: 2]",
    "python_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:\n        prev = None\n        curr = head\n\n        while curr:\n            next_elem = curr.next\n            curr.next = prev\n\n            prev = curr\n            curr = next_elem\n\n        return prev"
  },
  {
    "id": 21,
    "title": "Merge Two Sorted Lists",
    "category": "Linked List",
    "difficulty": "Easy",
    "time_complexity": "O(n + m)",
    "space_complexity": "O(1)",
    "loc": 25,
    "description": "We are given the heads of two sorted linked lists (list1 and list2).[cite: 2] We need to merge them into one single sorted list and return its head.[cite: 2] The new list should be made by splicing together the nodes of the first two lists.[cite: 2]",
    "solution_logic": "When we have to build or modify a linked list dynamically, a dummy node is your best friend.[cite: 2] It acts as a permanent anchor at the very beginning so we never accidentally lose track of where our new list starts.[cite: 2] Here is the game plan: we create a dummy node and a tail pointer that starts right there.[cite: 2] Then, we compare the current nodes of list1 and list2.[cite: 2] Whichever value is smaller gets attached to our tail.next, and we advance that list's pointer forward.[cite: 2] We repeat this comparison loop until one of the lists runs out of nodes.[cite: 2] Once one list is exhausted, we don't need to loop anymore—we can just attach whatever is left of the remaining list straight to our tail.next in one go.[cite: 2] Finally, we return dummy.next (skipping the dummy placeholder itself).[cite: 2] It runs in O(n + m) time and uses O(1) extra space.[cite: 2]",
    "python_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\n        temp = ListNode()\n        tail = temp\n\n        while list1 and list2:\n            if list1.val < list2.val:\n                tail.next = list1\n                list1 = list1.next\n            else:\n                tail.next  = list2\n                list2 = list2.next\n            \n            tail = tail.next\n\n        if list1:\n            tail.next = list1\n        elif list2:\n            tail.next = list2\n\n        return temp.next"
  },
  {
    "id": 143,
    "title": "Reorder List",
    "category": "Linked List",
    "difficulty": "Medium",
    "time_complexity": "O(n)",
    "space_complexity": "O(1)",
    "loc": 37,
    "description": "We are given the head of a singly linked list.[cite: 2] We need to reorder it so that it follows this specific alternating pattern: L0 -> Ln -> L1 -> Ln-1 -> L2 -> Ln-2 -> ....[cite: 2] We must do this in-place by modifying the nodes themselves, rather than just changing their values.[cite: 2]",
    "solution_logic": "This problem looks intimidating because it scrambles the list order, but it becomes completely manageable when you break it down into three classic linked list patterns you already know: Find the middle: Use the slow and fast pointer technique to find the exact midpoint of the list.[cite: 2] Reverse the second half: Take everything after the middle and reverse it using pointer manipulation (just like we did in Reverse Linked List).[cite: 2] Merge alternatingly: Interleave the nodes from the first half and the reversed second half one by one.[cite: 2] By splitting the list into two halves, reversing the tail, and zippering them back together, we can solve this cleanly in O(n) time and O(1) extra space.[cite: 2]",
    "python_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reorderList(self, head: Optional[ListNode]) -> None:\n        \"\"\"\n        Do not return anything, modify head in-place instead.\n        \"\"\"\n        if not head or not head.next:\n            return\n\n        p1 = head\n        p2 = head.next\n        while p2 and p2.next:\n            p1 = p1.next\n            p2 = p2.next.next\n\n        second = p1.next\n        prev = None\n        p1.next = None\n\n        while second:\n            nxt = second.next\n            second.next = prev\n            prev = second\n            second = nxt\n\n        first = head\n        second = prev\n        while second:\n            temp1 = first.next\n            temp2 = second.next\n            first.next = second\n            second.next = temp1\n            first = temp1\n            second = temp2"
  }
]